#Return Values 

e = math.exp(1.0)
height = radius * math.sin(radians)

def area(radius):
    a = math.pi * radius**2
    return a
  
def area(radius):
    return math.pi * radius**2
  
 def absolute_value(x):
     if x < 0:
        return - x
      if x > 0:
        return x
